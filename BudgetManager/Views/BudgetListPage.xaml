<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="BudgetManager.Views.BudgetPage"
    xmlns:vm="clr-namespace:BudgetManager.ViewModels"
    Title="Budgets">

    <Grid RowDefinitions="Auto, *">
        <Grid RowDefinitions="Auto, Auto">
            <Grid Style="{StaticResource PageHeader}"
                  RowDefinitions="Auto,Auto">
                
                <Grid ColumnDefinitions="Auto,*,Auto">
                    <HorizontalStackLayout>
                        <Button Style="{StaticResource Circular}"
                            Text="&#xf104;"
                            FontFamily="fa-solid"
                            TextColor="{StaticResource TextPrimary}"
                            Command="{Binding PreviousMonthCommand}"/>

                        <Label Text="{Binding MonthDisplay}"
                           TextColor="White"
                           FontSize="18"
                           VerticalOptions="Center" />

                        <!-- Next Month Button -->
                        <Button Grid.Column="2"
                            Style="{StaticResource Circular}"
                            Text="&#xf105;"
                            FontFamily="fa-solid"
                            TextColor="{StaticResource TextPrimary}"
                            Command="{Binding NextMonthCommand}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Grid.Column="1" HorizontalOptions="End">
                        <Button Style="{StaticResource Circular}"
                            Text="&#xf067;"
                            FontFamily="fa-solid"
                            TextColor="{StaticResource TextPrimary}"
                            Command="{Binding GoToAddBudgetCommand}" />
                    </HorizontalStackLayout>
                </Grid>

                <Grid Grid.Row="1" ColumnDefinitions="*,*,*" Margin="12">
                    <VerticalStackLayout HorizontalOptions="Start">
                        <Label Text="Total Budget" HorizontalOptions="Center"/>
                        <Label
                            Text="{Binding TotalBudget, StringFormat='৳ {0:F2}'}" 
                            HorizontalOptions="Center" 
                            FontSize="16" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                        <Label Text="Expenses" HorizontalOptions="Center"/>
                        <Label
                            Text="{Binding TotalSpent, StringFormat='৳ {0:F2}'}"
                            HorizontalOptions="Center"
                            FontSize="16"
                            TextColor="{StaticResource Accent}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Column="2" HorizontalOptions="End">
                        <Label Text="Remaining" HorizontalOptions="Center"/>
                        <Label 
                            Text="{Binding RemainingAmount, StringFormat='৳ {0:F2}'}" 
                            HorizontalOptions="Center" 
                            FontSize="16"
                            TextColor="#4FC3F7" />
                    </VerticalStackLayout>
                </Grid>
            </Grid>
            
            <BoxView Grid.Row="1" HeightRequest="0.1"
                BackgroundColor="{DynamicResource Divider}"/>
        </Grid>

        <Grid Grid.Row="1" Style="{StaticResource PageBody}">
            <CollectionView ItemsSource="{Binding Budgets}">
                <CollectionView.EmptyView>
                    <Label Text="No budgets added this month yet. Tap the + button to create one!" 
                        FontSize="16"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        HorizontalTextAlignment="Center" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout>
                            <Border StrokeThickness="0" Padding="16">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:BudgetViewModel}}, Path=GoToBudgetDetailsCommand}"
                                            CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>

                                <Grid ColumnDefinitions="*,Auto,*" RowDefinitions="Auto,Auto,Auto,Auto">
                                    <!-- Budget Name -->
                                    <Label Grid.Row="0" Grid.Column="0"
                                        Text="{Binding Name}" 
                                        FontSize="18" 
                                        FontAttributes="Bold" />

                                    <!-- Budget Amount -->
                                    <Label Grid.Row="0" Grid.Column="2"
                                        Text="{Binding Amount, StringFormat='৳ {0:F2}'}" 
                                        FontSize="18" 
                                        FontAttributes="Bold"
                                        TextColor="{StaticResource Primary}"
                                        HorizontalOptions="End" />

                                    <!-- Progress Bar -->
                                    <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                                        Progress="{Binding ProgressPercentage}" />

                                    <!-- Spent and Remaining Labels (Below Progress Bar) -->
                                    <Label Grid.Row="2" Grid.Column="0"
                                        Text="{Binding FormattedSpent}"
                                        FontSize="12"
                                        TextColor="{StaticResource TextSecondary}"
                                        HorizontalOptions="Start"/>

                                    <Label Grid.Row="2" Grid.Column="1"
                                        Text="{Binding FormattedPercentage}"
                                        FontSize="12"
                                        FontAttributes="Bold"
                                        TextColor="{StaticResource TextSecondary}"
                                        HorizontalOptions="Center"
                                        Margin="0,0,0,2" />

                                    <Label Grid.Row="2" Grid.Column="2"
                                        Text="{Binding FormattedRemaining}"
                                        FontSize="12"
                                        TextColor="{StaticResource TextSecondary}"
                                        HorizontalOptions="End" />
                                </Grid>
                            </Border>

                            <BoxView HeightRequest="0.1"
                                BackgroundColor="{DynamicResource Divider}"/>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>